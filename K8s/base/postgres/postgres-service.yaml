apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: voting
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: pg
      protocol: TCP
      port: 5432
      targetPort: 5432
